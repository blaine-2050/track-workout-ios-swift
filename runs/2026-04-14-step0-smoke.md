# Run: Step 0 Smoke Test — 2026-04-14T23:50 PDT

**Platform:** track-workout-ios-swift @ `0d7feeb` (+ uncommitted package vendoring)
**Harness:** iOS Simulator, iPhone 12, iOS 26.2
**Xcode:** 26.2 (17C52)
**Bundle:** `com.athenia.TrackWorkout`

## Goal
Step 0: verify the toolchain works end-to-end — build → install → launch → screenshot. No UI driving this run.

## Steps

1. **Resolve package dependency.** `TrackWorkoutCore` Swift Package vendored into repo at `TrackWorkoutCore/`. Pbxproj relative path changed from `../../apple-shared/TrackWorkoutCore` to `../TrackWorkoutCore`. **PASS** (resolves after edit).
2. **Build.** `xcodebuild -project TrackWorkout/TrackWorkout.xcodeproj -scheme TrackWorkout -destination 'platform=iOS Simulator,name=iPhone 12' -configuration Debug build`. **PASS** — `** BUILD SUCCEEDED **`.
3. **Boot simulator.** `xcrun simctl boot "iPhone 12"`. **PASS** — booted in ~20s.
4. **Install.** `xcrun simctl install booted <app>.app`. **PASS**.
5. **Launch.** `xcrun simctl launch booted com.athenia.TrackWorkout`. **PASS** — pid 3962.
6. **Screenshot.** `xcrun simctl io booted screenshot runs/screens/2026-04-14-step0-01-launch.png`. **PASS** — [screenshot](screens/2026-04-14-step0-01-launch.png).

## Observations

- App launched cleanly. No crash, no obvious layout issues on iPhone 12.
- **Prior Core Data state persisted** across the monorepo→standalone migration. This is expected (simulator keeps app containers until wiped) but worth noting — fresh-install testing will require `xcrun simctl uninstall` first.
- **Stale-timer bug visible in the screenshot:** the history shows a workout "started on Wed, Feb 25 at 2:15 PM" with set/total elapsed times of `1160:35:37 hours` and `46 sec` per entry. That's ~48 days since last use — the per-workout or per-set elapsed calculation appears to include walltime gaps when the app wasn't running. Likely a `Date.now - startedAt` calculation that doesn't clamp to workout-active time.
- **"Elapsed: 69635:14"** label in green near the middle of the screen — unclear what this measures (set timer since last move select? looks like minutes:seconds). If that's a set timer continuing across 48 days, that's the same class of bug.
- History shows two older workouts (Feb 25 at 2:15 PM and 1:49 PM) with Lat Pull Down + Leg Press entries. Data integrity across migration intact.

## Next actions

- [ ] **Decide UI-driving path** before attempting workout-script 01. Options: (a) Claude Desktop computer use, (b) add accessibility IDs + XCUITest, (c) install `idb`.
- [ ] **Open issue for stale-timer behavior** — history should cap elapsed to workout-active window or format long gaps differently.
- [ ] **Decide on fresh-install protocol** for scripted runs — wipe app container before each run? Or test against seeded fixtures?

## Open question for user

The stale-timer observation is the first operational deficiency this loop has surfaced. Do you want it tracked as an issue in this repo, or escalated to `track-workout-core` as a UX invariant violation (since it's visible across platforms if they all calculate elapsed the same way)?
